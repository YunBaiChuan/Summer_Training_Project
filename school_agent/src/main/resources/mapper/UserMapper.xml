<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gxa.mapper.UserMapper">

    <resultMap id="UserMap" type="com.gxa.pojo.entity.User">
        <id property="id" column="id"/>
        <result property="nickname" column="nickname"/>
        <result property="phone" column="phone"/>
        <result property="password" column="password"/>
        <result property="avatarUrl" column="avatar_url"/>
        <result property="credit" column="credit"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <!-- 新增用户 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (nickname, phone, password, avatar_url, credit, create_time)
        VALUES (#{nickname}, #{phone}, #{password}, #{avatarUrl}, #{credit}, NOW())
    </insert>

    <!-- 根据 ID 查询 -->
    <select id="selectById" resultMap="UserMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <!-- 查询所有用户 -->
    <select id="selectAll" resultMap="UserMap">
        SELECT * FROM user ORDER BY create_time DESC
    </select>

    <!-- 条件查询用户 -->
    <select id="selectByCondition" resultMap="UserMap">
        SELECT * FROM user
        <where>
            <if test="nickname != null and nickname != ''">
                AND nickname LIKE CONCAT('%', #{nickname}, '%')
            </if>
            <if test="phone != null and phone != ''">
                AND phone LIKE CONCAT('%', #{phone}, '%')
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <!-- 根据手机号查询 -->
    <select id="selectByPhone" resultMap="UserMap">
        SELECT * FROM user WHERE phone = #{phone}
    </select>

    <!-- 更新用户信息 -->
    <update id="update">
        UPDATE user
        <set>
            <if test="nickname != null and nickname != ''">
                nickname = #{nickname},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="avatarUrl != null and avatarUrl != ''">
                avatar_url = #{avatarUrl},
            </if>
            <if test="credit != null">
                credit = #{credit},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 更新信用分 -->
    <update id="updateCredit">
        UPDATE user SET credit = #{credit} WHERE id = #{id}
    </update>

    <!-- 删除用户 -->
    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <!-- 批量删除用户 -->
    <delete id="deleteBatch">
        DELETE FROM user WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>